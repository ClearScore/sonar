# Publishing a Sonar Update

```shell
npm login
```
^ this will prompt for a username e.g. `peter.mouland`, password and email address

```shell
yarn publish
```
^ this will ask you what the new version should be, and commit the package bump

```shell
npm publish
```
^ this will push to npm
